---
import VideoIFrame from './_VideoIFrame.astro';

export interface Props {
  id: string;
}

const { id } = Astro.props as Props;
---

<VideoIFrame src={`https://www.youtube-nocookie.com/embed/${id}`} />
