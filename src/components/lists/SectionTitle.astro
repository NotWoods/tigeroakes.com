---
export interface Props {
  class?: string;
}

export const classes = 'text-3xl border-b py-1 uppercase';

const { class: className } = Astro.props as Props;
---

<h2 class:list={[classes, className]}>
  <slot />
</h2>
