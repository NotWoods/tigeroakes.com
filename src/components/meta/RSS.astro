---
export interface Props {
  href: string;
  title?: string;
}

const { href, title = 'Tiger Oakes' } = Astro.props as Props;

const url = new URL(href, Astro.canonicalURL);
---

<link rel="alternate" type="application/rss+xml" href={url.href} {title} />
